import { User } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export function ProfilePage() {
  return (
    <PlaceholderPage
      icon={User}
      title="Profile"
      description="Profile info, settings, and notification preferences are coming soon."
    />
  );
}
