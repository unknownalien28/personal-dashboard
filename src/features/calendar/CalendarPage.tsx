import { CalendarDays } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export function CalendarPage() {
  return (
    <PlaceholderPage
      icon={CalendarDays}
      title="Calendar"
      description="A monthly calendar with events, reminders, and deadlines is coming soon."
    />
  );
}
