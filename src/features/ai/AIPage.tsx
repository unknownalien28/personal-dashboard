import { Bot } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export function AIPage() {
  return (
    <PlaceholderPage
      icon={Bot}
      title="AI Assistant"
      description="A built-in chat to help organize tasks, summarize notes, and brainstorm is coming soon."
    />
  );
}
