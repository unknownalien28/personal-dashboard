import { FileText } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export function NotesPage() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Notes"
      description="Rich text notes with search, pinning, and tags are coming soon."
    />
  );
}
