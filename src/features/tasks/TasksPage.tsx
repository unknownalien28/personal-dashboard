import { CheckSquare } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export function TasksPage() {
  return (
    <PlaceholderPage
      icon={CheckSquare}
      title="Tasks"
      description="Task management is coming up next: create, edit, prioritize, and track progress."
    />
  );
}
